/* Narration manifest (DECISIONS B33). When the founder records a line from
   docs/NARRATION_SCRIPT.md, drop the file in app/audio/ and map its ID here:
       "w1l1-b0": "audio/w1l1-b0.mp3",
   Any ID present here plays the recording; everything else falls back to the
   browser's speech engine. Recordings win, always. */
const AUDIO_MANIFEST = {};
